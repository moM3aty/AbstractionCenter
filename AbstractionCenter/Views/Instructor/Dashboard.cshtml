@model IEnumerable<AbstractionCenter.Models.Entities.Batch>
@{
    ViewData["Title"] = "لوحة تحكم المحاضر";
}

<style>
    :root {
        --inst-bg: #f8fafc;
        --inst-surface: #ffffff;
        --inst-primary: #0b1120;
        --inst-primary-light: #1e293b;
        --inst-gold: #d4af37;
        --inst-gold-glow: rgba(212, 175, 55, 0.3);
        --inst-text: #0f172a;
        --inst-muted: #64748b;
        --inst-border: rgba(212, 175, 55, 0.2);
        --inst-radius: 24px;
        --inst-shadow: 0 15px 35px -10px rgba(11, 17, 32, 0.08);
        --inst-shadow-hover: 0 25px 50px -12px rgba(212, 175, 55, 0.15);
    }

    .dashboard-wrapper {
        padding-bottom: 60px;
    }

    .hero-dashboard {
        background: linear-gradient(135deg, var(--inst-primary), var(--inst-primary-light));
        border-radius: var(--inst-radius);
        padding: 50px;
        margin: 30px 0 50px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        position: relative;
        overflow: hidden;
        box-shadow: 0 20px 40px -10px rgba(11, 17, 32, 0.4);
        border: 1px solid rgba(212, 175, 55, 0.15);
    }

        .hero-dashboard::before {
            content: '';
            position: absolute;
            top: -100px;
            right: -50px;
            width: 350px;
            height: 350px;
            background: radial-gradient(circle, var(--inst-gold-glow) 0%, transparent 70%);
            border-radius: 50%;
        }

    .hero-content {
        position: relative;
        z-index: 10;
        color: white;
    }

        .hero-content h1 {
            font-size: 2.6rem;
            font-weight: 900;
            margin-bottom: 10px;
            color: white;
        }

            .hero-content h1 span {
                color: var(--inst-gold);
                text-shadow: 0 0 20px rgba(212,175,55,0.4);
            }

        .hero-content p {
            font-size: 1.1rem;
            color: #cbd5e1;
            max-width: 500px;
            line-height: 1.7;
            margin: 0;
        }

    .hero-stats {
        position: relative;
        z-index: 10;
        display: flex;
        gap: 20px;
    }

    .stat-badge {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        padding: 20px 30px;
        border-radius: 20px;
        text-align: center;
        backdrop-filter: blur(10px);
    }

        .stat-badge h3 {
            color: var(--inst-gold);
            font-size: 2rem;
            font-weight: 900;
            margin: 0;
            line-height: 1;
        }

        .stat-badge p {
            color: #cbd5e1;
            font-size: 0.95rem;
            margin: 8px 0 0 0;
            font-weight: 700;
        }

    .section-title {
        color: var(--inst-primary);
        font-size: 1.6rem;
        font-weight: 900;
        margin-bottom: 30px;
        display: flex;
        align-items: center;
        gap: 12px;
    }

        .section-title i {
            color: var(--inst-gold);
        }

    .courses-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
        gap: 30px;
    }

    .luxury-card {
        background: var(--inst-surface);
        border-radius: var(--inst-radius);
        overflow: hidden;
        box-shadow: var(--inst-shadow);
        border: 1px solid #e2e8f0;
        transition: all 0.4s ease;
        display: flex;
        flex-direction: column;
        position: relative;
    }

        .luxury-card:hover {
            transform: translateY(-10px);
            box-shadow: var(--inst-shadow-hover);
            border-color: var(--inst-gold);
        }

    .luxury-card-img {
        height: 200px;
        position: relative;
        overflow: hidden;
        background: var(--inst-primary-light);
    }

        .luxury-card-img img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.7s ease;
            opacity: 0.9;
        }

    .luxury-card:hover .luxury-card-img img {
        transform: scale(1.08);
        opacity: 1;
    }

    .luxury-card-img .overlay {
        position: absolute;
        inset: 0;
        background: linear-gradient(to top, rgba(11,17,32,0.9) 0%, transparent 100%);
    }

    .card-badge {
        position: absolute;
        top: 15px;
        right: 15px;
        background: rgba(11, 17, 32, 0.8);
        color: var(--inst-gold);
        padding: 6px 15px;
        border-radius: 30px;
        font-size: 0.85rem;
        font-weight: 800;
        backdrop-filter: blur(5px);
        border: 1px solid rgba(212, 175, 55, 0.3);
        z-index: 2;
    }

    .luxury-card-content {
        padding: 25px;
        flex-grow: 1;
        display: flex;
        flex-direction: column;
        z-index: 5;
    }

    .luxury-card-title {
        font-size: 1.3rem;
        font-weight: 900;
        color: var(--inst-primary);
        margin-bottom: 5px;
        line-height: 1.5;
    }

    .luxury-batch-name {
        color: #3b82f6;
        font-size: 1rem;
        font-weight: 800;
        margin-bottom: 15px;
    }

    .luxury-card-meta {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 25px;
        color: var(--inst-muted);
        font-size: 0.9rem;
        font-weight: 700;
    }

    .luxury-btn {
        background: #f8fafc;
        color: var(--inst-primary);
        border: 2px solid #e2e8f0;
        padding: 12px 20px;
        border-radius: 14px;
        text-align: center;
        font-weight: 800;
        font-size: 1rem;
        text-decoration: none;
        transition: all 0.3s ease;
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 10px;
        margin-top: auto;
    }

    .luxury-card:hover .luxury-btn {
        background: var(--inst-primary);
        color: var(--inst-gold);
        border-color: var(--inst-primary);
    }

    @@media (max-width: 992px) {
        .hero-dashboard

    {
        flex-direction: column;
        text-align: center;
        gap: 30px;
        padding: 40px 20px;
    }

    .hero-stats {
        justify-content: center;
        flex-wrap: wrap;
    }

    }
</style>

<div class="dashboard-wrapper">
    <div class="hero-dashboard">
        <div class="hero-content">
            <h1>مرحباً بك، <span>@ViewData["InstructorName"]</span></h1>
            <p>مساحتك الأكاديمية الملكية. أدر دفعاتك التدريبية، ارفع محتواك، وصمم رحلة تعليمية استثنائية لطلابك.</p>
        </div>
        <div class="hero-stats">
            <div class="stat-badge">
                <h3>@(Model?.Count() ?? 0)</h3>
                <p>دفعات مسندة</p>
            </div>
            <div class="stat-badge">
                <h3><i class="fa-solid fa-certificate" style="color: var(--inst-gold);"></i></h3>
                <p>محاضر معتمد</p>
            </div>
        </div>
    </div>

    <h2 class="section-title"><i class="fa-solid fa-laptop-code"></i> الدفعات المسندة إليك</h2>

    <div class="courses-grid">
        @if (Model != null && Model.Any())
        {
            foreach (var batch in Model)
            {
                <div class="luxury-card">
                    <div class="luxury-card-img">
                        <div class="card-badge">
                            @if (batch.Status == AbstractionCenter.Models.Entities.BatchStatus.OpenForRegistration)
                            {
                                <span><i class="fa-solid fa-door-open"></i> تسجيل مفتوح</span>
                            }
                            else
                            {
                                <span style="color:#94a3b8;"><i class="fa-solid fa-lock"></i> قيد التنفيذ / مغلقة</span>
                            }
                        </div>
                        @if (!string.IsNullOrEmpty(batch.Course?.ImageUrl))
                        {
                            <img src="@batch.Course.ImageUrl" alt="@batch.Course.Title" />
                        }
                        else
                        {
                            <div style="width:100%; height:100%; display:flex; align-items:center; justify-content:center; background: #1e293b;">
                                <i class="fa-solid fa-layer-group" style="font-size: 5rem; color: rgba(212,175,55,0.2);"></i>
                            </div>
                        }
                        <div class="overlay"></div>
                    </div>

                    <div class="luxury-card-content">
                        <div class="luxury-card-title">@batch.Course?.Title</div>
                        <div class="luxury-batch-name"><i class="fa-solid fa-users-rectangle"></i> @batch.BatchName</div>
                        <div class="luxury-card-meta">
                            <span><i class="fa-regular fa-calendar-days" style="color:var(--inst-gold)"></i> تبدأ: @batch.StartDate.ToString("dd MMM yyyy")</span>
                        </div>
                        <a href="/Instructor/ManageBatch/@batch.Id" class="luxury-btn">
                            إدارة المنهج والمحتوى <i class="fa-solid fa-arrow-left-long"></i>
                        </a>
                    </div>
                </div>
            }
        }
        else
        {
            <div style="grid-column: 1 / -1; background: white; border-radius: var(--inst-radius); padding: 80px 20px; text-align: center; border: 2px dashed #e2e8f0; box-shadow: var(--inst-shadow);">
                <div style="width: 90px; height: 90px; background: rgba(212, 175, 55, 0.1); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 20px; border: 1px solid rgba(212, 175, 55, 0.3);">
                    <i class="fa-solid fa-folder-open" style="font-size: 2.5rem; color: var(--inst-gold);"></i>
                </div>
                <h3 style="color: var(--inst-primary); font-weight: 900; font-size: 1.6rem; margin-bottom: 10px;">لا توجد دفعات مسندة إليك حالياً</h3>
                <p style="color: var(--inst-muted); font-size: 1.05rem;">تواصل مع إدارة المركز لربط حسابك بالدفعات لتتمكن من رفع المادة العلمية.</p>
            </div>
        }
    </div>
</div>